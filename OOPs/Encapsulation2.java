package OOPs;

public class Encapsulation2 {
     public static void main(String[] args) {
        Encapsultaion car = new Encapsultaion();
        car.setBrand("Toyota");
        System.out.println(car.getBrand());

        // if we don't want to set any properties so we just remove the method of that property and also vice versa in getters(get);
     }
}
